spring.config.import=file:env.properties
spring.application.name=w2d5exam
# SERVER CONFIG
server.port=3001
# DB CONFIG (Le prime per usare diversi da quelli classici con parametri)
#spring.datasource.url=jdbc:postgresql://localhost:5432/${PG_DB_NAME}
#spring.datasource.username=${PG_USERNAME}
#spring.datasource.password=${PG_PASSWORD}
spring.datasource.url=jdbc:postgresql://localhost:5432/w3d5exam
spring.datasource.username=postgres
spring.datasource.password=1234
spring.datasource.driver=org.postgresql.Driver
# HIBERNATE CONFIG
spring.jpa.hibernate.ddl-auto=update
# CLOUDINARY CONFIG
cloudinary.name=${CLOUDINARY_NAME}
cloudinary.key=${CLOUDINARY_KEY}
cloudinary.secret=${CLOUDINARY_SECRET}
# JWT CONFIG
jwt.secret=${JWT_SECRET}